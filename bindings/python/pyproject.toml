[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aq-database"
version = "0.1.0"
description = "Thin Python binding for the aq_database shared library"
requires-python = ">=3.11"
