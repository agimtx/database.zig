[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "database-zig"
version = "0.1.0"
description = "Thin Python binding for the database.zig shared library"
requires-python = ">=3.11"
